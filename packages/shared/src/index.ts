// export하여 admin과 client등 다른 곳에서 사용할 유틸리티 함수 작성

// import _ from 'lodash';

// export const capitalize = (str: string) => {
//   return _.capitalize(str);
// };

export const sampleFunc = (str: string) => {
  return "this is input string = " + str;
};
